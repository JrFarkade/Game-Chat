{
  "name": "gamechat-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker & Durable Objects backend for GameChat real-time voice and signaling",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260921.1",
    "typescript": "^5.7.3",
    "wrangler": "^4.136.1"
  }
}
